services:
  database:
    image: postgres:latest
    container_name: recipes
    restart: always
    environment:
      - POSTGRES_PASSWORD=${DBPASSWORD}
      - POSTGRES_USER=${DBUSER}
    expose:
      - 5432
    ports:
      - '5432:5432'
