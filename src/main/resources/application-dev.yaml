spring:
  application:
    name: cooking

  datasource:
    url: ${DBURL}
    username: ${DBUSER}
    password: ${DBPASSWORD}

  jpa:
    hibernate:
      ddl-auto: "create"
    show-sql: true
  flyway:
    enabled: false
